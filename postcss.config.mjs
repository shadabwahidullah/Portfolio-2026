/** PostCSS configuration: Tailwind generates utilities, Autoprefixer adds vendor prefixes. */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
